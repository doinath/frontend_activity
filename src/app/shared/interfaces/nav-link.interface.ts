/** @description A single link used by the section nav rail and the footer nav. */
export interface NavLink {
  label: string;
  path: string;
  /** Single-letter glyph shown in the floating section-nav rail. */
  icon: string;
}
